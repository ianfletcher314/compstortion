# TODO

## Completed
- [x] Make the highlight color match the pedal color
- [x] When reordering pedals, reorder the toggle buttons as well
- [x] Create the ability to save presets (Supabase with user accounts)

## Future Ideas
- [ ] Add URL-based preset sharing (encode settings in URL for easy sharing)
- [ ] Add local storage fallback for guest users to save presets locally
- [ ] Preset export/import as JSON files
